"""
algorithms/ppo/policy_network.py
پیکربندی شبکه‌ی سیاست/ارزش. به‌جای طراحی معماری سفارشی (که برای این مسئله‌ی
122->75 بعدی ضرورتی ندارد)، از MlpPolicy پیش‌فرض stable-baselines3 با یک
policy_kwargs مستند استفاده می‌شود؛ در صورت نیاز به معماری پیچیده‌تر
(مثلاً لایه‌های جدا برای بخش سرور/سرویس state) فقط PPO_POLICY_KWARGS باید
تغییر کند - بقیه‌ی کد به آن وابسته نیست.
"""

PPO_POLICY_KWARGS = dict(
    net_arch=dict(pi=[256, 256], vf=[256, 256]),  # شبکه‌ی سیاست و ارزش هرکدام ۲ لایه‌ی ۲۵۶تایی
)
